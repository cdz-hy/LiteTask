<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- 提醒相关权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".LiteTaskApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiteTask"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LiteTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 提醒广播接收器 -->
        <receiver
            android:name=".reminder.ReminderReceiver"
            android:exported="false"
            android:enabled="true" />
        
        <!-- 开机广播接收器 -->
        <receiver
            android:name=".reminder.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- 悬浮提醒服务 (作为 Activity 模式的补充) -->
        <service
            android:name=".reminder.FloatingReminderService"
            android:exported="false"
            android:enabled="true" />

        <!-- 强提醒 Activity (闹钟模式) -->
        <activity
            android:name=".reminder.ReminderActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.LiteTask.Reminder" />
        
        <!-- ========== 小组件 ========== -->
        
        <!-- 任务列表小组件 Provider -->
        <receiver
            android:name=".widget.TaskListWidgetProvider"
            android:exported="true"
            android:label="@string/widget_task_list_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.litetask.app.widget.ACTION_TOGGLE_TASK" />
                <action android:name="com.litetask.app.widget.ACTION_REFRESH_TASK_LIST" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_task_list_info" />
        </receiver>
        
        <!-- 任务列表小组件 Service -->
        <service
            android:name=".widget.TaskListWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        
        <!-- 甘特图小组件 Provider -->
        <receiver
            android:name=".widget.GanttWidgetProvider"
            android:exported="true"
            android:label="@string/widget_gantt_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.litetask.app.widget.ACTION_REFRESH_GANTT" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_gantt_info" />
        </receiver>
        
        <!-- 甘特图小组件 Service -->
        <service
            android:name=".widget.GanttWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        
        <!-- 截止提醒小组件 Provider -->
        <receiver
            android:name=".widget.DeadlineWidgetProvider"
            android:exported="true"
            android:label="@string/widget_deadline_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.litetask.app.widget.ACTION_REFRESH_DEADLINE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_deadline_info" />
        </receiver>
        
        <!-- 截止提醒小组件 Service -->
        <service
            android:name=".widget.DeadlineWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        
        <!-- 小组件刷新广播接收器 -->
        <receiver
            android:name=".widget.WidgetRefreshReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.CONFIGURATION_CHANGED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
